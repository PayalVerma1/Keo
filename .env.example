# Core persistence
DATABASE_URL=postgresql://keo:keo@postgres:5432/keo?schema=public
REDIS_CLIENT=redis://redis:6379

# Application security
NEXTAUTH_SECRET=replace-with-a-long-random-secret
NEXTAUTH_URL=http://localhost:3000
JWT_SECRET=replace-with-a-long-random-secret

# Optional: enables AI explanations in verification reports
GEMINI_API_KEY=

# GitHub integration (used only by the PR workflow / callback worker)
GITHUB_TOKEN=
GITHUB_REPOSITORY=
GITHUB_SHA=
GITHUB_PR_NUMBER=
