name: Deploy Workers to Azure

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/azure-workers.yml"
      - "package.json"
      - "pnpm-lock.yaml"
      - "tsconfig.json"
      - "tsconfig.workers.json"
      - "prisma/**"
      - "src/lib/config/**"
      - "src/lib/streams/**"
      - "src/lib/workers/**"
      - "src/lib/modules/websocket/**"
  workflow_dispatch:

# Azure App Service accepts only one active OneDeploy operation per app. Queue
# newer worker deployments rather than allowing a second deployment to return 409.
concurrency:
  group: azure-keo-workers-deploy
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma Client
        run: pnpm exec prisma generate

      - name: Type check
        run: pnpm exec tsc --noEmit

      - name: Build Workers
        run: pnpm run build:workers

      - name: Create Azure Runtime Package
        run: |
          rm -rf azure-workers-package
          mkdir azure-workers-package
          cp package.json package-lock.json azure-workers-package/
          cp -R dist azure-workers-package/dist
          # Copy prisma generated client if it exists
          if [ -d "node_modules/.prisma" ]; then
            mkdir -p azure-workers-package/node_modules/.prisma
            cp -R node_modules/.prisma azure-workers-package/node_modules/
          fi
          cd azure-workers-package
          npm install --omit=dev --ignore-scripts
          # Create a startup script so Azure runs from the correct directory
          echo '#!/bin/sh' > startup.sh
          echo 'cd /home/site/wwwroot' >> startup.sh
          echo 'NODE_ENV=production node dist/src/lib/workers/index.js' >> startup.sh
          chmod +x startup.sh

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: keo-workers
          publish-profile: ${{ secrets.AZURE_WORKERS_PUBLISH_PROFILE }}
          package: azure-workers-package
