# This workflow belongs on the APPLICATION repo (the product you monitor), not on Keo.
#
# Keo is only the API: fingerprint + scoring. Developers copy
# examples/app-repo/.github/workflows/keo-pr-score.yml into their own GitHub repo.
# Their PRs then call YOUR deployed Keo. This file does not run on Keo pull requests.

name: KEO PR verification (Keo repo — do not use)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Score customer app PRs with examples/app-repo/.github/workflows/keo-pr-score.yml"
